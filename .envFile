PORT=5000

DATABASE_USER=postgres
DATABASE_PASSWORD=1234
DATABASE_HOST=localhost
DATABASE_PORT=5432
DATABASE_NAME=Test


DATABASE_URL="postgresql://${DATABASE_USER}:${DATABASE_PASSWORD}@${DATABASE_HOST}:${DATABASE_PORT}/${DATABASE_NAME}?schema=public"

# MongoDB Configuration
MONGO_USER= # Add your MongoDB username if authentication is enabled
MONGO_PASSWORD= # Add your MongoDB password if authentication is enabled
MONGO_HOST=localhost
MONGO_PORT=27017
MONGO_DATABASE=giva

# For local MongoDB without authentication
MONGO_URI="mongodb://${MONGO_HOST}:${MONGO_PORT}/${MONGO_DATABASE}"

# For MongoDB with authentication (uncomment and fill if needed)
# MONGO_URI="mongodb://${MONGO_USER}:${MONGO_PASSWORD}@${MONGO_HOST}:${MONGO_PORT}/${MONGO_DATABASE}?authSource=admin"


# ImageKit Configuration
IMAGEKIT_PUBLIC_KEY=public_b/ToDi7Bej+UY49T6Sa1eMFO/bU=your_public_key
IMAGEKIT_PRIVATE_KEY=private_X7kfYfS/Evu9ifEr7unsTLW8klY=
IMAGEKIT_URL_ENDPOINT=https://ik.imagekit.io/odtre34u0
